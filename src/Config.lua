return {
    PLUGIN_NAME = "WorldMessages",
    DISPLAY_NAME = "World Messages",

    TAG_NAME = "WorldMessage",
    STORAGE_TAG_NAME = "WorldMessageStorage",

    BILLBOARD_MAX_DISTANCE = 400, -- studs

    -- The amount of studs to push the camera back when zooming in on a
    -- WorldMessage billboard.
    PUSHBACK_FROM_FOCUS = 10, -- studs
}
